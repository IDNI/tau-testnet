# --- Handle Invalid Input ---
# This rule uses bitvector arithmetic.
# o1[t]: 0 if both inputs are zero, otherwise original amount.
always ((((i0[t] | i1[t]) = { #b0 }:bv) ? o1[t] = { #b0 }:bv : o1[t] = (i1[t] + { #b0 }:bv))).
